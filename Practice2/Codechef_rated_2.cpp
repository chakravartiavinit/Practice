#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        int n, a, b;
        cin >> n;
        int arr[n], index[n];
        map<int, int> mp;
        for (int i = 0; i < n; i++)
        {
            cin >> a;
            mp.insert(a, i);
        }
        for(int i=0;i<n;i++)
        {
            
        }
    }
    return 0;
}
