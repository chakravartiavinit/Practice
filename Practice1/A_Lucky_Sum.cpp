#include <bits/stdc++.h>
using namespace std;
set<long long int> Lucky;
long long NextLucky(long long a,long long b)
{
    while(a<)
    
}
int main(int argc, char const *argv[])
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    long long l, r;
    cin >> l >> r;

    return 0;
}