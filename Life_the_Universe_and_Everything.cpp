#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    while (1)
    {
        int input;
        cin >> input;
        if (input == 42)
        {
            break;
        }
        cout << input << "\n";
    }
    return 0;
}