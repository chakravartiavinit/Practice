#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int arr[6][6];
    for (int i = 1; i < 6; ++i)
    {
        for (int j = 1; j < 6; ++j)
        {
            cin >> arr[i][j];
        }
    }
    for (int i = 1; i < 6; ++i)
    {
        for (int j = 1; j < 6; ++j)
        {
            if(j==1)
            {
            
            }
        }
    }
    return 0;
}